{
  "scripts": {
  "watch" : "npx tailwindcss -i ./style.css -o ./main.css --watch",
  "build" : "npx tailwindcss -i ./style.css -o ./main.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.16"
  }
}
